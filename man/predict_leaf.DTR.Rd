% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MOGTree-package.R, R/predict_leaf.R
\name{predict_leaf.DTR}
\alias{predict_leaf.DTR}
\title{Additional documentation for \code{predict_leaf.DTR()}}
\usage{
predict_leaf.DTR(treeout, newdata)
}
\arguments{
\item{treeout}{An object outputted from DTRtree function.}

\item{newdata}{New data containing H (history).}
}
\value{
\code{predict_leaf.DTR()} returns an integer vector of terminal node ids,
one per row of \code{newdata}.
}
\description{
Additional documentation for \code{predict_leaf.DTR()}

Predict optimal treatment using the output from DTRtree.
}
\examples{
set.seed(1)
n <- 60
H <- data.frame(
  X1 = rnorm(n),
  X2 = rnorm(n)
)
A <- sample(1:3, n, replace = TRUE)
Y1 <- 1 + 0.5 * (H$X1 > 0) + c(0.9, 0.3, -0.2)[A] + rnorm(n, sd = 0.3)
Y2 <- 0.5 - 0.3 * (H$X1 > 0) + c(-0.2, 0.4, 0.8)[A] + rnorm(n, sd = 0.3)
w <- seq(0, 1, by = 0.5)

fit <- DTRtree(
  Ys = cbind(Y1, Y2),
  A = A,
  H = H,
  depth = 2,
  minsplit = 10,
  w_vec = cbind(w, 1 - w),
  weight_combine = "mean",
  lambda = 0.01
)

predict_leaf.DTR(fit, H[1:6, , drop = FALSE])
}
