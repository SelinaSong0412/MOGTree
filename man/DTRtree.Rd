% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MOGTree-package.R, R/MO_Tree.R
\name{DTRtree}
\alias{DTRtree}
\title{Additional documentation for \code{DTRtree()}}
\usage{
DTRtree(
  Ys,
  mus.hat = NULL,
  A,
  H,
  pis.hat = NULL,
  mus.reg = NULL,
  depth = 5,
  minsplit = 20,
  w_vec,
  m.method = c("AIPW", "randomForest"),
  weight_combine,
  lambda = 0.01
)
}
\arguments{
\item{Ys}{A matrix of outcome of interest, each column record one outcome.}

\item{mus.hat}{Optional list of estimated counterfactual mean matrices, one
per outcome.}

\item{A}{A vector of observed treatment options.}

\item{H}{A matrix of covariates before assigning final treatment, excluding previous treatment variables.}

\item{pis.hat}{Estimated propensity score matrix.}

\item{mus.reg}{Regression-based conditional mean outcome.}

\item{depth}{Maximum tree depth.}

\item{minsplit}{Minimal node size.}

\item{w_vec}{A matrix of weight vectors; each row defines one outcome-weight
combination used in the gain calculation.}

\item{m.method}{Method for calculating estimated conditional mean.}

\item{weight_combine}{Rule used to aggregate gain values across the rows of
\code{w_vec}. Current options are \code{"max"} and \code{"mean"}.}

\item{lambda}{Minimum gain required to continue splitting a node.}
}
\value{
\code{DTRtree()} returns a data frame describing the fitted binary tree.
The columns are \code{node}, \code{X}, \code{cutoff}, \code{mEy}, and \code{group}, where \code{X} and
\code{cutoff} record the selected splitting variable and cutoff for each
internal node and \code{group} indexes terminal nodes.
}
\description{
a tree-based reinforcement learning (T-RL) method to directly
estimate optimal DTRs in a multi-stage multi-treatment setting. At
each stage, T-RL builds an unsupervised decision tree that directly handles
the problem of optimization with multiple treatment comparisons, through a
purity measure constructed with augmented inverse probability weighted estimators.
}
\examples{
set.seed(1)
n <- 80
H <- data.frame(
  X1 = rnorm(n),
  X2 = rnorm(n)
)
A <- sample(1:3, n, replace = TRUE)
Y1 <- 1 + 0.6 * (H$X1 > 0) + c(0.8, 0.2, -0.3)[A] + rnorm(n, sd = 0.3)
Y2 <- 0.5 - 0.4 * (H$X1 > 0) + c(-0.1, 0.5, 0.7)[A] + rnorm(n, sd = 0.3)
w <- seq(0, 1, by = 0.25)

fit <- DTRtree(
  Ys = cbind(Y1, Y2),
  A = A,
  H = H,
  depth = 2,
  minsplit = 10,
  w_vec = cbind(w, 1 - w),
  weight_combine = "mean",
  lambda = 0.01
)

fit
}
